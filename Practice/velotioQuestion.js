function arrayProduct(array) {

}


function cardGame(N, K) {
    let array = new Array(N);
    array.fill(1, 0, N);

    let result = 0

    for(let i = 0; i < N; i++) {
        
    }

    return result;
}

cardGame(3, 6);