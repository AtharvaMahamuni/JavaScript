console.log("Hello, Atharva here!");

// This is the old way to run the javascript code. Using browser console.
// But now becuase we have the V8 engine with the nodeJS which acts as a runtime environment for JS.
// We can run JS code on the command line or on the terminal. All the compilation and interpretation happens
// with the NodeJS.